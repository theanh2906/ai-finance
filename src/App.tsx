'use client';

// This file is kept for compatibility but is no longer used by Next.js.
// The entry point is app/page.tsx → src/components/AppShell.tsx
export { default } from './components/AppShell';
